// Utility to send notifications (stub)
module.exports = function sendNotification(type, recipient, message) {
  // TODO: Integrate with Email/SMS/WhatsApp
  console.log(`Send ${type} to ${recipient}: ${message}`);
};
